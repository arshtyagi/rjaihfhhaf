{
  "name": "apollo-counter",
  "version": "1.0.0",
  "description": "Apollo.io search URL → pipeline_total count via authenticated session",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "install-browsers": "npx playwright install chromium --with-deps"
  },
  "dependencies": {
    "express": "^4.18.2",
    "playwright": "^1.44.0",
    "imap": "^0.8.19",
    "mailparser": "^3.6.9",
    "dotenv": "^16.4.5",
    "node-cache": "^5.1.2"
  }
}
